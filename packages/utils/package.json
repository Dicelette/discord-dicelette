{
	"name": "@dicelette/utils",
	"version": "2.28.1",
	"private": true,
	"description": "",
	"type": "module",
	"main": "dist/index.js",
	"repository": {
		"type": "git",
		"url": "https://github.com/Mara-Li/discord-dicelette.git"
	},
	"scripts": {
		"build": "tsc --project tsconfig.json && tsc-alias -p tsconfig.json",
		"prebuild": "rimraf dist",
		"test": "vitest run",
		"tsc": "tsc --project tsconfig.json --skipLibCheck --noEmit && tsc-alias -p tsconfig.json"
	},
	"dependencies": {
		"@types/semver": "^7.7.0",
		"enmap": "^6.0.4",
		"random-js": "^2.1.0"
	},
	"devDependencies": {
		"@types/papaparse": "^5.3.15",
		"papaparse": "^5.4.1",
		"semver": "^7.7.2",
		"strip-ansi": "^7.1.2"
	}
}
